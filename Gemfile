source 'http://rubygems.org'

gem 'rails', '3.1.0.rc4'

gem 'haml'
gem 'haml-rails'
gem 'formtastic'
gem 'compass'
gem 'configatron'
gem 'jquery-rails'
gem 'headjs-rails'

gem 'mongoid'
gem 'bson_ext'
gem 'omniauth'

gem 'typhoeus'

# Asset template engines
gem 'sass'
gem 'coffee-script'
gem 'uglifier'

# uncomment if your production env doesn't have a javascript interpreter
# https://github.com/cowboyd/therubyracer
# group :production do
#   gem 'therubyracer'
# end

# other useful gems:
# gem 'nokogiri' # fast xml/html parser
# gem 'will_paginate', '~> 3.0.pre2'
# gem 'devise' # if you want email signup as well as oauth
# gem 'koala' # for facebook api
# gem 'memcache-client'

group :development do
  gem 'capistrano'
  gem 'ruby-debug19'
  gem 'colored'
  gem 'autotest'
  gem 'autotest-rails-pure'
  gem 'autotest-growl'
  gem 'autotest-fsevent' # for filesystem event monitoring in OS X (http://www.bitcetera.com/products/autotest-fsevent)
end

group :test do
  gem 'turn', :require => false # Pretty printed test output
  gem 'rspec-rails'
  gem 'mongoid-rspec'
  gem 'database_cleaner'
  gem 'shoulda'
  gem 'factory_girl_rails'
  gem 'cucumber-rails'
  gem 'capybara'
end
